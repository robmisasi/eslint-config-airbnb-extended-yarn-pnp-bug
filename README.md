# eslint-config-airbnb-extended-yarn-pnp-bug

Run `yarn lint` to see module resolution issue when accessing unlisted
**@typescript-eslint/parser** dependency.

These are resolvable by installing **@typescript-eslint/parser**.
